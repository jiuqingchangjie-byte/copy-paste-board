# 开发待办

本文件是后续开发的统一入口，依据用户确认的需求持续更新。最近更新：**2026-09-12**。

**1.5.0 已于 2026-09-12 发布**，完整预览、JSON 格式化、自定义快捷键、收藏库与可配置存储均已实现，尚有 **4 项功能待开发**。功能实现、版本发布与实机验收分别记录，未覆盖场景不标记为通过。证据见 [既有验收记录](docs/ACCEPTANCE.md)、[完整预览验收](docs/PREVIEW.md)、[JSON 格式化验收](docs/JSON_PREVIEW.md) 和 [自定义快捷键验收](docs/CUSTOM_SHORTCUT.md)。

## 已确认的产品边界

- **普通历史**默认保留 10 条，可配置 **1–50 条**；普通历史的模型、设置、导入与旧数据迁移均遵守此上限。
- **收藏库独立保存，不占普通历史额度，不受 50 条限制**；支持一级文件夹、多选移动/取消收藏及清空。单条 8 MiB 规则继续保留。
- 以捕获到的复制先后顺序维护队列。满额后每新增一条不同内容，只淘汰相对最早的一条；连续新增逐条淘汰。
- 重复内容去重并移到最新位置；历史粘贴不改变复制顺序；无效或超大内容不挤占队列。
- 旧版本超过 50 条时，保留最新 50 条；旧设置大于 50 自动收敛到 50 并保存。调小上限立即保留最新 N 条。
- 普通历史保持轻量队列，不采用总字节数提前淘汰；长期内容整理通过独立收藏库完成。
- macOS 首次授权由用户完成；后续正常更新保持应用身份和安装路径，不主动重置权限。权限被用户撤销、系统重置或签名身份变化时，不能保证免授权。
- 每轮实机测试先执行版本核对：运行路径、版本、构建编号、启动时的可执行文件哈希和签名均须匹配。

## 高优先级

- [ ] **应用排除与隐私模式**：按稳定的 bundle ID 排除来源；提供仅内存模式、可选保存期限、暂停状态持久化。
  - 验收：排除源不落盘，重启后仍暂停；模式切换明确处理已有磁盘记录；到期清理不暗中导回已删内容。不能承诺凭内容识别所有密码。

## 中优先级

- [ ] **类型筛选与快速定位**：全部 / 文本 / 图片 / 文件；筛选与关键词组合；评估数字快捷选择。
  - 验收：筛选不改变原历史顺序，空结果和选中项删除后边界正常，数字输入不能误触粘贴。
- [ ] **删除撤销**：单条删除、清空提供短时间撤销，不为日常删除反复弹确认框。
  - 验收：明确撤销时间窗口；新复制、上限调整、重启与隐私清理不会恢复不应保留的记录；撤销后仍不超过 50 条。

## 后续

- [ ] **格式保留**：保留 HTML / RTF 与纯文本表示；明确“原格式 / 纯文本”的选择体验。
  - 验收：网页和文档格式可恢复，不支持富文本的目标可使用纯文本；图文、多表示去重与单条大小规则一致。

## 1.5.0 已完成

- [x] **独立收藏库**：历史星标快捷收藏、顶部及菜单栏直达；收藏不占普通历史额度、没有 50 条上限，去重并支持重启恢复。
- [x] **目录与多选管理**：全部/未分类/自建目录、重命名、删除目录移回未分类；支持多选移动/取消收藏、当前目录清空与全库清空，范围确认明确。
- [x] **可配置存储位置**：历史、收藏与设置一起后台迁移，校验后切换；旧目录保留副本，目标占用或失败不覆盖，位置缺失不自动创建空历史。
- [x] **耐久保存与故障恢复**：历史和设置刷新后原子替换，收藏事务保存；强制终止测试进程后恢复已提交数据，未提交删除回滚。未实施整机断电测试。
- [x] **自行验收与安装**：127 项 Swift 测试、8 项流程测试通过；星标、目录、多选移动/取消、全库清空、实际位置迁移、迁移后的强制退出恢复、复制均已实测。构建 `58F39433` 已安装，原历史、设置及授权保留。见 [产品方案与验收](docs/FAVORITES_AND_STORAGE.md)。

- [x] **1.5.0 发布**：代码、[Release](https://github.com/jiuqingchangjie-byte/copy-paste-board/releases/tag/v1.5.0)、中英文说明、源码包及 macOS arm64 安装包与校验文件。

## 1.4.0 已完成

- [x] **自定义快捷键**：录制、候选保存、恢复默认与错误提示；检查系统组合和排他注册，保存失败保留旧注册；重启恢复并同步菜单及提示。
- [x] **测试与安装**：117 项 Swift 测试、8 项流程测试通过；录制、实际跨进程冲突、写入失败回退、重启注册和恢复默认已实测。构建 `0C084538` 已安装并核对数据与授权。
- [ ] **快捷键实体键盘验收**：新组合在其他应用呼出/关闭历史、旧组合不再呼出；其他布局及全屏/多桌面。CUA 未取得可靠跨应用呼出证据，详见 [快捷键验收](docs/CUSTOM_SHORTCUT.md)。

- [x] **1.4.0 发布**：代码、[Release](https://github.com/jiuqingchangjie-byte/copy-paste-board/releases/tag/v1.4.0)、中英文说明、源码包及 macOS arm64 安装包与校验文件。

## 1.3.1 已完成

- [x] **完整预览内一键 JSON 格式化**：新增格式化与查看原文按钮；保持数字精度、转义、重复键及顺序；错误输入提示并保留原文。查看不改剪贴板或历史，显式选区复制沿用原逻辑。
- [x] **自行测试与安装**：105 项 Swift 测试、8 项流程测试通过；系统剪贴板采集到实际预览格式化、原文切换、片段复制与失败提示均实测。构建 `5F9A6B94` 已安装，运行身份、原数据及授权核对通过。详见 [JSON 格式化验收](docs/JSON_PREVIEW.md)。
- [x] **代码提交与推送**：提交 `b0917c0` 已推送到 main，标签 `v1.3.1` 指向同一提交。
- [x] **1.3.1 Release 发布**：[Release](https://github.com/jiuqingchangjie-byte/copy-paste-board/releases/tag/v1.3.1) 已发布并设为 Latest，包含中英文说明、源码包、macOS arm64 安装包及各自的校验文件。
- [x] **发布附件校验**：GitHub 显示的安装包及 `.zip.sha256` 文件摘要均与本地一致；发布包复用已验收构建 `5F9A6B94`，固定自签名、未公证。

## 1.3.0 已完成

- [x] **悬浮全文预览**：默认等待 1 秒，可配置 0.2–5 秒或关闭；在条目旁显示全文，保留换行并支持滚动。
- [x] **片段复制入历史**：预览中选中文字后通过 ⌘C、按钮或右键菜单复制；仅显式复制改写系统剪贴板，继续遵守去重、条数与单条大小限制。
- [x] **原图查看与置顶**：图片右键查看原图，支持缩放、原始大小、适合窗口和独立置顶；提供标题栏及顶部拖动手柄。置顶窗口不等同于持久化收藏。
- [x] **键盘与焦点隔离**：预览回车不触发历史粘贴，搜索空格正常输入；Esc 按条目 ID 返回原选择和焦点。
- [x] **模块拆分与旧设置兼容**：按 History、Preview、Preferences、WindowChrome 划分职责；旧设置默认启用 1 秒悬浮。
- [x] **自动化回归与本机验证**：93 项 Swift 测试、8 项构建/安装流程测试通过；全文滚动、片段复制、选择恢复、搜索空格、原图缩放与置顶、配置保存已有本机证据。
- [x] **固定路径安装**：构建 `634425E1` 的运行版本、哈希与签名核对通过；原历史、已有设置和授权保留。
- [x] **代码与 Release 发布**：提交 `af0bba7` 已推送到 main；[v1.3.0 Release](https://github.com/jiuqingchangjie-byte/copy-paste-board/releases/tag/v1.3.0) 已发布，包含中英文说明、源码包、macOS arm64 安装包及对应校验文件，上传摘要与本地一致。

## 完整预览待实机验收

每轮先核对正在运行的版本、构建编号、路径、启动哈希和签名；重编译或重启后重新核对。步骤与已有证据见 [完整预览验收](docs/PREVIEW.md)。

- [ ] **实体鼠标悬浮**：默认 1 秒与自定义延时、移出取消、连续换条目、移入预览滚动/选字；当前延时与取消逻辑已有 AppKit 自动化证据。
- [ ] **实体触摸板手势**：双指辅助点按打开图片菜单，双指缩放原图；普通双指滚动不误触菜单。
- [ ] **实体鼠标拖动置顶图片**：标题栏和顶部手柄均可改变屏幕位置，缩放与置顶状态保持；CUA 调用未确认坐标变化，不能视为实测通过。
- [ ] **其他系统与窗口环境**：多显示器、屏幕边缘、全屏/不同桌面、其他 macOS 版本验证预览定位、置顶与关闭后焦点。

## 既有已完成工程

- [x] 历史模型、设置与界面统一 50 条硬上限，补齐逐条淘汰和旧数据迁移测试。
- [x] 为历史和设置加入格式版本；旧格式可读，更新版本格式不被破坏性重建。
- [x] 增加“数据与恢复”：保留内存记录、重试保存、损坏原文件留存后重建；恢复时尊重本次删除和清空。
- [x] 增加启动身份记录和 `scripts/verify-running.sh`，测试前识别旧进程。
- [x] 增加固定路径安装脚本与发布者签名、公证脚本。脚本存在不代表公证已完成。
- [x] 文本编辑回车复验：修复构建 `2F555699` 已安装且版本核对通过；用户于 2026-09-10 确认“回车已能正常粘贴一次”，双击亦已确认可用。
- [x] 按用户选择提供已编译的 GitHub 应用下载包，固定自签名、未公证；无需源码打包或 Apple 开发者会员。

## 其他待验收与可选工作

- [ ] 完成所有真实应用与系统场景验收；逐项以证据勾选，未覆盖场景保持待验收。
- [ ] 在另一台 Mac 验证编译应用包的首次打开、辅助功能授权，以及后续同身份更新后的授权保留。
- [ ] 可选后续：如将来需要再配置 Developer ID 和公证；不是当前 GitHub 分发的前置条件。
